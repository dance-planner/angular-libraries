export interface IImageLinkWithDetailsLink {
  imageLink: string;
  detailsLink: string;
}

export interface IStylingData {
  bgColor: string;
  imageWith: string;
  imagePadding: string;
  frameMargin: string;
  framePadding: string;
  frameBackgroundColor: string;
  imageBackgroundColor: string;
}
